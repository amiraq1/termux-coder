# تقرير دمج Orchestrator مع Agent

## الحالة

تم تنفيذ دمج تدريجي لمسار `AgentOrchestrator` داخل المستودع `amiraq1/termux-coder` مع الإبقاء على مسار `Agent.run_turn` القديم هو الافتراضي. لم يتم حذف المسار القديم أو استبداله قسرًا؛ ويمكن تفعيل المسار الجديد عبر Feature Flag.

## ما تم تغييره

| المكون | التغيير |
|---|---|
| `config.py` | إضافة `orchestrator_enabled`، ويُفعّل عبر `TERMUX_CODER_ORCHESTRATOR=1` |
| `core/orchestrator_adapter.py` | إضافة Adapter يحافظ على توجيه `fast/smart` ويستبعد أدوات التعديل من fast |
| `core/agent.py` | إضافة `_run_turn_orchestrated()` مع حفظ الرسائل والجلسة وخريطة المستودع وميزانية السياق |
| `core/orchestrator.py` | إضافة حفظ الرسائل، محضر سياق اختياري، موافقة UI، استعادة tool calls النصية، وتصحيح أولوية DENY |
| `providers/openai_compat.py` | دعم إرسال مخططات الأدوات وجمع native tool calls أثناء البث، مع متغير تعطيل للتوافق |
| `tools/edit.py` | منع طلب موافقة ثانية عند اعتماد العملية مسبقًا من Orchestrator |
| `tools/shell.py` | منع تكرار موافقة الأوامر مع تسجيل مصدر الموافقة |
| `tools/gittool.py` | منع تكرار موافقات Git الداخلية عند وجود موافقة Orchestrator |
| `ui/cli.py` و`ui/app.py` | عرض الرفض والموافقات وحالة Orchestrator للمستخدم |
| `tests/test_orchestrator_integration.py` | اختبارات Adapter لتوجيه الأدوات واختيار tier بين الجولات |

## آلية التفعيل

المسار القديم يبقى فعالًا افتراضيًا:

```bash
termux-coder --workspace ~/myproject
```

لتجربة المسار الجديد:

```bash
export TERMUX_CODER_ORCHESTRATOR=1
termux-coder --workspace ~/myproject
```

ولتعطيل native tool calling مع مزود لا يدعمه:

```bash
export TERMUX_CODER_NATIVE_TOOLS=0
```

عند تعطيل native tool calling، يبقى مسار Recovery قادرًا على استخراج استدعاءات الأدوات المطبوعة كنص، بشرط أن يخرج النموذج بنية JSON قابلة للاستخراج.

## ضمانات الدمج

يحافظ المسار الجديد على ترتيب رسائل API المطلوب: رسالة المستخدم، ثم رسالة المساعد التي تحمل `tool_calls`، ثم نتائج الأدوات، ثم رسالة المساعد التالية. كما تُحفظ الرسائل الجديدة في `SessionStore` فور إضافتها، ويُستخدم `ContextAssembler` لتقليص السياق المرسل للنموذج دون حذف السجل الكامل من الجلسة.

تُفحص صلاحية الأداة من `PolicyEngine` قبل التنفيذ، ويظل `DENY` قرارًا نهائيًا. أما العمليات التي تتطلب موافقة فتُعرض عبر عقد UI الموجود، وتُمنع الموافقة الثانية داخل أدوات patch وshell وGit عند تمريرها من Orchestrator.

## نتائج التحقق

تم تنفيذ:

```bash
python3 -m compileall -q src tests
python3 -m pytest -q
git diff --check
```

والنتيجة:

| الفحص | النتيجة |
|---|---:|
| الاختبارات السابقة | 131 ناجحة، 1 متجاوزة |
| الاختبارات بعد الدمج | **133 ناجحة، 1 متجاوزة** |
| أخطاء الصياغة | صفر |
| أخطاء whitespace في diff | صفر |

## حدود الاعتماد الحالية

هذا الدمج **تجريبي وقابل للتراجع** وليس اعتمادًا إنتاجيًا نهائيًا. لم يتم اختبار اتصال فعلي بمزود خارجي في هذه البيئة، كما أن توافق native tool calling يظل معتمدًا على دعم المزود لنفس مخطط Chat Completions. لذلك يجب تجربة `TERMUX_CODER_NATIVE_TOOLS=0` عند استخدام مزود لا يقبل `tools`.

كما أن عرض موافقة patch في Orchestrator يستخدم المعاملات الخام للترقيع قبل تشغيل الأداة، وليس Diff محسوبًا مسبقًا. الأداة نفسها تظل مسؤولة عن التحقق من الترقيع وإنتاج Diff قبل الكتابة، لكن تحسين تجربة العرض يتطلب مرحلة لاحقة تضيف Preview موحدًا قبل الموافقة.

## خطة التحقق قبل جعل المسار الجديد افتراضيًا

1. تجربة المسار الجديد على مشروع اختبار صغير في وضع `READONLY`، ثم في وضع `ASK`.
2. اختبار طلب قراءة، ثم تعديل ملف، ثم تشغيل اختبار، ثم تراجع.
3. اختبار رفض المستخدم، وانتهاء الموافقة، وتغير arguments، وإلغاء الدورة أثناء طلب النموذج وأثناء الأداة.
4. اختبار الاستئناف من SessionStore بعد انقطاع العملية.
5. اختبار مزود native tools، ثم مزود recovery-only مع `TERMUX_CODER_NATIVE_TOOLS=0`.
6. بعد نجاح ذلك، يمكن جعل Feature Flag افتراضيًا في إصدار Beta، مع إبقاء متغير رجوع إلى المسار القديم.

## القرار

الدمج الحالي مناسب كـ **P2 Integration Alpha**. لا يوصى بحذف المسار القديم أو جعل Orchestrator افتراضيًا قبل تنفيذ اختبارات الجهاز والمزود الحقيقي المذكورة أعلاه.
