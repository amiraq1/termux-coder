# تقرير إعادة تصميم واجهة TUI

## الحالة

تم تنفيذ إعادة التصميم داخل `src/termux_coder/ui/app.py` مع الحفاظ على منطق الوكيل وعقود Safe Preview وVerification وعدم تغيير مسار الموافقات أو الأدوات.

## التغييرات المنفذة

| المجال | التغيير |
|---|---|
| الرأس | إضافة شريط علوي يعرض اسم المشروع والنموذج ووضع الأمان |
| الشاشة الفارغة | إضافة بطاقة بداية عربية بأمثلة طلبات عملية |
| Timeline | إضافة شريط نشاط ثابت يعرض READY وTHINKING وPLANNING وEXECUTING وVERIFYING وAWAITING APPROVAL |
| القراءة | الحفاظ على أحداث READ مع عداد تراكمي للملفات والأسطر |
| الأدوات | عرض TOOL عند بدء التنفيذ ونتائج الأدوات الحالية |
| الحالات | عرض الحالة الحالية مع عداد التوكنات في شريط status |
| الهاتف | إبقاء شجرة الملفات مخفية افتراضيًا، وإظهارها عبر Ctrl+T |
| الإدخال | حقل إدخال مثبت ومختصر Ctrl+P للتركيز عليه |
| الاختصارات | الإبقاء على Ctrl+O لتوسيع Diff وShift+Tab لتغيير الوضع |
| العربية | تمرير نصوص الواجهة كـRich Text/markup=False حيث يلزم لتقليل تشوه الرموز |
| Safe Preview | لم يتغير عقد الموافقة أو Diff أو التحقق الأمني |

## التحقق

```text
compileall: passed
pytest: 149 passed, 1 skipped
 git diff --check: passed
```

## ملاحظة الإصدار

التغييرات موجودة محليًا ولم تُرفع إلى GitHub بعد. يجب تشغيل TUI فعليًا على Termux مع `python -m termux_coder run --workspace ...` لمراجعة القياسات على شاشة الهاتف قبل إنشاء commit، لأن اختبار المحطة لا يحاكي كل أحجام الطرفية.
